useState-> Jab tum state change karte React JS mein jisse jaise kuch counter ke count ki value, tum js se usku nai dikha sakte React JS mein cuz Tum direct website se interact nai kar sakte (DOM) se jaisa tum in general karte they before using JS woh changes React JS ich karta useState ke through 

React JS State management mein Help karta 

const [num,setNum]=useState(0);
ismein num only read value hai basically aur setNum only write aur useState mein jo value daalte tum woh initial value rehti...

setNum async hai toh tum pehle console karke setNum karke phir console karey toh possibly tumaku same ich value bata sakti for once 

radio mein name same rehna id different rehna 